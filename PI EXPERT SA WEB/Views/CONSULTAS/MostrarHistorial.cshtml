@*@model IEnumerable<PI_EXPERT_SA_WEB.Models.Group<string, >>*@

@using  PI_EXPERT_SA_WEB.Models
@model List<Group<string, PROYECTO>>

@*Lista de requerimientos de un proyecto es especiifico*@

    <table class="table table-bordered table-striped" id="tabla">
        <tr>
            <th>
                Nombre Proyecto
            </th>
            @*<th>
                Duración Estimada
            </th>
            <th>
                Duración Real
            </th>
            <th>
                Diferencia
            </th>*@
        </tr>

        @foreach (var group in Model)
        {


            //var diff = item.modeloRequerimiento.duracionReal - item.modeloRequerimiento.duracionEstimada;

            <tr>
                <td>
                    @Html.DisplayFor(modelItem => group.Key)
                </td>
                @*@foreach (var book in group.Values)
                {
                    <tr><td>@book.</td><td>@book.Title</td><td>@book.Price.ToString("c")</td></tr>
                }*@
            @*<td>
                @Html.DisplayFor(modelItem => item.modeloRequerimiento.duracionEstimada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.modeloRequerimiento.duracionReal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => diff)
            </td>*@
            </tr>
        }
    </table>


